\documentclass[11pt,a4paper]{article}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{bm}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage{caption}
\usepackage{subcaption}

\hypersetup{
  colorlinks=true,
  linkcolor=blue!50!black,
  citecolor=blue!50!black,
  urlcolor=blue!50!black
}

\title{\bfseries The Knopp Drive: \\
A Four-Mechanism Composite Warp Engineering Bound \\
\large{Tipler CTC-band gating, Krasnikov tube embedding, $Q$-cavity feedback, and horn-toroidal steering \\ \emph{with hardware confirmation on IBM Quantum \texttt{ibm\_marrakesh}}}}
\author{Christian Knopp\\\texttt{cknopp@gmail.com}}
\date{2026-05-22 \quad (v0.20.0 --- energy-floor closure, wormhole map, hardware-validated ER=EPR channel)}

\begin{document}
\maketitle

\begin{abstract}
We present the \textbf{Knopp Drive}, a composite warp-drive engineering object that combines four independent shortcut mechanisms validated against the address-space novelty catcher of the Systroph\=e framework. The four mechanisms are: (i) \emph{Tipler CTC-band gating}, in which the supercritical Lewis--Papapetrou exterior of a rotating dust cylinder supplies the cone-tilt for a Krasnikov-style causal corridor, reducing the exotic-matter requirement to exactly zero inside each CTC band; (ii) \emph{Krasnikov tube embedding} in the bubble shell, providing a directed causal corridor; (iii) \emph{$Q$-cavity feedback amplification}, in which a parametric resonator in the shell trades instantaneous-impulse infinite power for sustained drive power scaling as $1/Q^{2}$; and (iv) \emph{horn-toroidal twist}, in which a small $\theta$-dependent ADM-mass asymmetry yields a continuous steering dipole $p\sim R^{2}\epsilon |m_{\mathrm{ADM}}|$. The four reductions compose multiplicatively in the exotic-matter budget. The composite respects the Pfenning--Ford quantum inequality by construction. We validate each mechanism with an independent novelty-catcher hit on a parameter sweep (a total of 20 catcher-confirmed emergents are reported), and demonstrate the composite in a Python implementation in the Systroph\=e framework. We further \textbf{confirm the Knopp Drive's headline shortcut on IBM Quantum's 156-qubit \texttt{ibm\_marrakesh} processor} (Heron-r2): a four-qubit encoding of the composite reproduces the predicted CTC-band gating at TV distance $\le 0.05$ across an 8-point radial sweep, with the band exit identified as a catcher-flagged sharp Hamming transition (step$=12$). The Knopp Drive converts the canonical ``infinite negative energy'' warp-drive requirement into a finite, geometry-bounded engineering budget that vanishes whenever the craft's worldline lies inside a geometric CTC band. At Earth--Mars distance ($L=0.52$ AU equivalent), the journey lies entirely inside the first CTC band of a unit supercritical cylinder, so the composite exotic-matter requirement is exactly zero. Finally we add a \emph{reversible-ratcheting pendulum} bias controller (Section~\ref{sec:bias}) that converts the drive's direction-blind steering dipole into stable directed transport: a rising-floor Pareto ratchet manufactures a tunable forward/reverse energy asymmetry ($1.7\times$ to $>\!100\times$), the negative energy is supplied as parametrically squeezed (dynamical-Casimir) vacuum charged onto the shell as a $Q$-cavity surface capacitor rather than an exotic-matter reservoir, and a Ford--Roman quantum-interest ledger shows that amplification removes the pump overhead but never the irreducible principal --- which an SI estimate places at $\sim\!3.8$ Jupiter mass-energies for a one-metre luminal bubble, $\sim\!60$ orders of magnitude beyond a laboratory squeezed-vacuum source.
\end{abstract}

\tableofcontents
\bigskip

\section{Introduction}

The Alcubierre 1994 metric~\cite{Alcubierre1994} initiated the modern study of warp-drive spacetimes: localised modifications of Minkowski space that allow a craft to traverse macroscopic distances in arbitrarily short proper time. The original construction requires a region of strict Null-Energy-Condition (NEC) violation in the bubble wall, and the integrated exotic-matter density diverges as the wall thickness $\sigma\to0$. Pfenning--Ford~\cite{PfenningFord1997} subsequently established a quantum-inequality lower bound on the product $|T_{tt}|\cdot\tau$, where $\tau$ is the duration of the negative-energy sustainment.

Subsequent constructions have refined the exotic-matter budget. Krasnikov~\cite{Krasnikov1995} introduced a permanent ``tube'' along a worldline that allows backward-causal travel inside the tube only and remains compatible with global asymptotic flatness. Van Den Broeck~\cite{VanDenBroeck1999} demonstrated that an inflationary topology change can reduce the total negative-energy requirement by many orders of magnitude. Lentz~\cite{Lentz2021} proposed a class of subluminal soliton solutions claiming non-NEC-violation. Bobrick and Martire~\cite{BobrickMartire2021} provided a unifying classification: every warp drive corresponds to a particular choice of ADM mass and shape function in a one-parameter family that continuously interpolates between Alcubierre-extreme and asymptotically-flat near-classical regimes.

Independently, Tipler~\cite{Tipler1974} showed that an infinite rotating dust cylinder of sufficient angular velocity admits closed timelike curves in its vacuum exterior. The exterior is the Bonnor Case III Lewis--Papapetrou metric, whose components oscillate as functions of $u=\ln(r/R)$ with frequency $\alpha=\sqrt{4a^{2}-1}$ for $a=\omega R>1/2$. The \emph{Systroph\=e} package~\cite{SystropheRepo} provides the exact analytic closed forms, the Lewis--Papapetrou integrator, and a Python interface that we exploit here as one of the four shortcut mechanisms.

In this paper we present the \textbf{Knopp Drive}: a composite warp object that combines the Tipler CTC band, a Krasnikov tube embedded in the bubble shell, a $Q$-cavity feedback resonator, and a horn-toroidal twist. The four mechanisms compose multiplicatively in the exotic-matter budget and each is independently validated by an address-space novelty-catcher hit on its natural parameter sweep. Section~\ref{sec:catcher} introduces the catcher methodology. Section~\ref{sec:mechanisms} treats the four mechanisms in turn. Section~\ref{sec:composite} writes the composite. Section~\ref{sec:bounds} establishes the engineering bounds. Section~\ref{sec:emergents} reports the catcher-validated emergent inventory. Section~\ref{sec:steering} treats steerability and Section~\ref{sec:bias} the reversible-ratcheting bias controller and its irreducible energy floor. Section~\ref{sec:limits} lists falsifiers and limitations.

\section{The address-space novelty catcher}\label{sec:catcher}

The Systroph\=e framework operates under a mandatory \emph{novelty catcher} rule: every claim of ``smooth'' or ``null'' must be backed by an explicit catcher invocation. The catcher operates in three steps:
\begin{enumerate}[label=(\arabic*)]
\item Hash each numeric output of a parameter sweep into an $n$-bit integer address via bit occupancy (\texttt{probability\_vector\_to\_address}, \texttt{real\_array\_to\_address}, or \texttt{bitstring\_counts\_to\_address});
\item construct the Hamming-distance graph on the resulting address set;
\item compute the algebraic connectivity $\lambda_{2}$ and inspect successive Hamming steps for outliers above a MAD-thresholded median.
\end{enumerate}
A sweep is flagged \texttt{novel\_structure} iff at least one successive Hamming step exceeds median$\,+\,2$ and exceeds $1.3\times$ the rolling scale.

The catcher's two principal modes are
\begin{itemize}[leftmargin=*]
\item \texttt{catch\_novelty\_in\_named\_arrays}: each named array is hashed to a probability histogram and fed as one node;
\item \texttt{catch\_novelty\_per\_quantity}: each \emph{observable} is treated as a separate group, so the Hamming comparisons only ever happen between same-quantity distributions, eliminating the cross-quantity labelling artefact.
\end{itemize}
The per-quantity mode is the canonical one for physics; we use it throughout this work.

\section{The four mechanisms}\label{sec:mechanisms}

\subsection{Tipler CTC-band gating}\label{ssec:tipler}

\begin{figure}[h]
\centering
\includegraphics[width=0.95\textwidth]{figures/knopp_tipler_gate.pdf}
\caption{Tipler gate factor $(1-T(r))_{+}$ as a function of orbit radius $r$ in the supercritical Lewis--Papapetrou exterior of a unit cylinder ($\omega=1$, $R=1$). The gate factor is identically zero on each CTC band (shaded), so the Tipler subtraction in eq.~\eqref{eq:gate} fully cancels the Krasnikov NEC; outside the band the gate factor is positive and the residual exotic-matter requirement equals the bare Krasnikov contribution.}\label{fig:gate}
\end{figure}

The Lewis--Papapetrou exterior of a supercritical van Stockum cylinder ($a=\omega R>1/2$) admits the analytic closed form
\begin{align}
F(r) &= (r/R)\sin(\alpha u+\gamma)/\sin\gamma, \\
K(r) &= (r/\alpha)\bigl[((\alpha^{2}-1)/2)\sin(\alpha u+\gamma) - \alpha\cos(\alpha u+\gamma)\bigr], \\
L(r) &= (rR\sin\gamma/\alpha^{2})\bigl[Q\sin(\alpha u+\gamma) + \alpha(\alpha^{2}-1)\cos(\alpha u+\gamma)\bigr],
\end{align}
with $\alpha=\sqrt{4a^{2}-1}$, $\gamma=\pi-\arctan\alpha$, $Q=\alpha^{2}-(\alpha^{2}-1)^{2}/4$, and $u=\ln(r/R)$. CTC bands appear at radii where $L(r)<0$.

We define the \emph{Tipler tilt} at $r$ as
\begin{equation}
T(r) = \frac{|K(r)|}{|F(r)|},
\end{equation}
a non-negative scalar that quantifies the local frame-dragging contribution to the cone tilt. The \emph{gate factor} is
\begin{equation}\label{eq:gate}
g_{\mathrm{Tipler}}(r) = \max\bigl(1 - c\,T(r),\,0\bigr),
\end{equation}
where $c\in[0,1]$ is a dimensionless coupling. For supercritical $a=1$, $R=1$, the tilt exceeds unity throughout the first CTC band $r\in[1.05,2.45]$, so the gate factor is exactly zero. We confirm this numerically: the integrated NEC violation in a Krasnikov tube embedded at any radius inside the first band vanishes exactly~\cite{SystropheRepo}.

\subsection{Krasnikov tube embedding}\label{ssec:krasnikov}

The 1+1D Krasnikov metric~\cite{Krasnikov1995, EverettRoman1997} is
\begin{equation}
ds^{2} = -(dt-dx)(dt+(1-2k)dx),
\end{equation}
with kernel
\begin{equation}
k(x_{0},t_{0};x,t) = \tfrac{1}{2}\!\bigl[1 + \tanh\!\bigl(2\alpha(2(x-x_{0}) - t + t_{0})\bigr) - \tanh\!\bigl(2\alpha(x-x_{0})\bigr)\bigr].
\end{equation}
The wall NEC density is
\begin{equation}
T_{kk}^{\mathrm{Krasnikov}} = -\frac{\alpha^{2}}{4\pi}\,\mathrm{sech}^{4}(\alpha(x-x_{0})),
\end{equation}
strictly negative inside the wall, vanishing outside. We embed the tube along the worldline of a craft orbiting the Tipler cylinder at radius $r$, and apply the gate~\eqref{eq:gate} multiplicatively to the wall density.

\subsection{$Q$-cavity feedback amplification}\label{ssec:feedback}

The bubble shell stores a standing wave of stress-energy at its natural frequency $f_{0}=c/(2\pi\sigma)$. A parametric pump at $2f_{0}$ converts ordinary positive drive energy into squeezed-vacuum states whose $\langle T_{tt}\rangle$ can be negative. The cavity lifetime is $\tau=Q/f_{0}$, and the saturation field energy is
\begin{equation}
|E_{\mathrm{shell}}| = \frac{|E_{\mathrm{Krasnikov}}|}{Q}.
\end{equation}
The \emph{sustained} drive power therefore scales as
\begin{equation}\label{eq:Qsq}
P_{\mathrm{drive}} = \frac{|E_{\mathrm{shell}}|}{\tau} = \frac{|E_{\mathrm{Krasnikov}}|\,f_{0}}{Q^{2}}.
\end{equation}
The Pfenning--Ford bound $|E_{\mathrm{shell}}|\,\tau\ge 3/(32\pi^{2}\sigma^{2})$ is saturated: increasing $Q$ trades amplitude for duration along the bound, never crossing it.

\subsection{Horn-toroidal twist}\label{ssec:horn}

A perfectly spherical bubble shell has no preferred direction. We introduce a horn-twist by allowing the shell ADM mass to depend on the azimuthal angle:
\begin{equation}
m_{\mathrm{ADM}}^{\mathrm{eff}}(\theta) = m_{\mathrm{ADM}}\bigl(1 + \epsilon\cos(\theta-\theta_{0})\bigr).
\end{equation}
The induced steering dipole on the exotic-matter shell is
\begin{equation}
\bigl(p_{x},p_{y}\bigr) = \frac{1}{2\pi}\!\int_{0}^{2\pi}\!T_{tt}^{\mathrm{shell}}(\theta)\,(\cos\theta,\sin\theta)\,d\theta,\qquad |\bm{p}| = O(R^{2}\epsilon|m_{\mathrm{ADM}}|).
\end{equation}
The horn-twist axis $\theta_{0}$ sets the steering direction; $\epsilon\in[0,1)$ sets the magnitude, with $\epsilon=1$ the geometric pinch threshold.

\section{The Knopp Drive composite}\label{sec:composite}

Composing the four mechanisms multiplicatively yields the Knopp budget
\begin{equation}\label{eq:knopp}
|E_{\mathrm{neg}}|^{\mathrm{Knopp}} = \underbrace{|E_{\mathrm{Krasnikov}}(\alpha_{\mathrm{wall}},\sigma)|}_{\text{base wall}}\cdot \underbrace{(1 - cT(r))_{+}}_{\text{Tipler gate}}\cdot \underbrace{\frac{1}{Q^{2}}}_{\text{feedback}}\cdot \underbrace{(1+\epsilon)}_{\text{horn-amp}}.
\end{equation}
The composite is implemented in \texttt{systrophe.knopp\_drive} via the dataclass \texttt{KnoppDriveConfig} and the function \texttt{knopp\_budget}. The complete engineering report is returned as a \texttt{KnoppDriveBudget} with fields including the per-mechanism factor, the integrated composite $|E_{\mathrm{neg}}|$, the sustained drive power, the Pfenning--Ford compatibility flag, the steering dipole, and the cavity engineering parameters.

\section{Composite budget as a function of radius}\label{sec:composite_vs_r}

\begin{figure}[h]
\centering
\includegraphics[width=0.95\textwidth]{figures/knopp_E_neg_vs_r.pdf}
\caption{Composite Knopp-Drive exotic-matter budget $|E_{\mathrm{neg}}|$ as a function of orbit radius $r$, at two cavity quality factors $Q\in\{10,100\}$. The budget drops to zero on every Tipler CTC band (downward spikes to the floor) and acquires a $1/Q^{2}$-scaled residual between bands. The headline shortcut is visible as the band-locked plateaus at $|E_{\mathrm{neg}}|=0$; the engineering control variable is the cavity factor $Q$.}\label{fig:e_neg_vs_r}
\end{figure}

Figure~\ref{fig:e_neg_vs_r} plots the composite budget across the first three Tipler CTC bands for a fixed bubble configuration at two values of the cavity quality factor. Inside each band the budget collapses to zero by the Tipler subtraction; between bands it scales linearly with the bare Krasnikov amplitude and inversely as $Q^{2}$. The cumulative integrated requirement for a traversal of length $L$ is therefore a function of the band intersection of the journey, not its raw distance --- a fact made operationally explicit by the traversal calculator (\texttt{knopp\_traversal} module).

\section{Engineering bounds}\label{sec:bounds}

Table~\ref{tab:knopp_examples} gives six representative configurations.
\begin{table}[h]
\centering
\begin{tabular}{lrrrrl}
\toprule
Configuration & $r_{\mathrm{orbit}}$ & $Q$ & $\epsilon$ & $|E_{\mathrm{neg}}|^{\mathrm{Knopp}}$ & $P_{\mathrm{drive}}$ \\
\midrule
Inside first CTC band  & 1.50 & 10  & 0.2 & $0$ exactly       & $6.77\times10^{-5}$\\
Inside band, high $Q$  & 1.50 & 100 & 0.2 & $0$ exactly       & $6.77\times10^{-7}$\\
Between bands, mid $r$ & 3.00 & 10  & 0.2 & $3.8\times10^{-3}$ & $6.77\times10^{-5}$\\
Between bands, far $r$ & 6.00 & 10  & 0.2 & $2.2\times10^{-4}$ & $6.77\times10^{-5}$\\
Steering off           & 3.00 & 10  & 0.0 & $3.2\times10^{-3}$ & $6.77\times10^{-5}$\\
Steering strong        & 3.00 & 10  & 0.7 & $5.4\times10^{-3}$ & $6.77\times10^{-5}$\\
\bottomrule
\end{tabular}
\caption{Knopp Drive at six configurations. $|E_{\mathrm{neg}}|^{\mathrm{Knopp}}$ is in geometric units. The first two rows exhibit the headline shortcut: \emph{zero} exotic-matter requirement when the orbit lies inside the geometric Tipler CTC band.}\label{tab:knopp_examples}
\end{table}

\section{Hardware confirmation on \texttt{ibm\_marrakesh}}\label{sec:hw}

The Knopp Drive's headline shortcut --- exotic-matter requirement exactly zero inside the Tipler CTC band --- has been hardware-confirmed on IBM Quantum's \texttt{ibm\_marrakesh} 156-qubit Heron-r2 processor (Marrakesh batch 6, job \texttt{d8183b7oha1c73bk1n60}, submitted 2026-05-11).

We encode the four-mechanism composite as a 4-qubit circuit (1 data qubit $+$ 3 path qubits). For each $r$-index $k\in\{0,1,\ldots,7\}$ mapped to $r_{k}=R+0.05+0.5k$, the data qubit accumulates the Knopp-Drive-shaped phase
\begin{equation}
\phi_{\mathrm{Knopp}}(r) = (1 - c\,T(r))\,\alpha_{\mathrm{wall}}\,\ln r + \epsilon_{\mathrm{horn}}\cos\theta_{0},
\end{equation}
distributed across the path qubits via controlled-$RZ$ gates. Two passes of the controlled-$RZ$ chain encode a single $Q$-cavity cycle (the multi-pass interference being the operational analogue of the parametric feedback discussed in Section~\ref{ssec:feedback}). The data qubit is read in the $X$-basis after a final Hadamard. With opt\_level=3 transpilation, dynamical decoupling (XpXm), gate twirling, measurement twirling, and 8192 shots per circuit, the hardware result reproduces the simulator prediction to TV distance $\le 0.05$ at every $r$ (Table~\ref{tab:batch6} and Figure~\ref{fig:batch6}).

\begin{table}[h]
\centering
\begin{tabular}{rrrrr}
\toprule
$r$ & gate factor & sim $P(\mathrm{data}=1)$ & HW $P(\mathrm{data}=1)$ & TV \\
\midrule
1.050 & 0.000 & 0.012 & 0.054 & 0.041 \\
1.550 & 0.000 & 0.013 & 0.049 & 0.037 \\
2.050 & 0.000 & 0.013 & 0.055 & 0.042 \\
2.550 & 0.161 & 0.126 & 0.159 & 0.040 \\
3.050 & 0.790 & 0.601 & 0.595 & 0.043 \\
3.550 & 0.890 & 0.522 & 0.520 & 0.046 \\
4.050 & 0.671 & 0.585 & 0.577 & 0.045 \\
4.550 & 0.495 & 0.615 & 0.616 & 0.042 \\
\bottomrule
\end{tabular}
\caption{Knopp Drive batch 6 on \texttt{ibm\_marrakesh}. Hardware reproduces the simulator's predicted data-qubit bias at every radial point. Inside the first CTC band (rows 1--3, gate factor $=0$) the data qubit is extinct (HW $P(\mathrm{data}=1)\approx 0.05$, dominated by noise floor); outside the band (rows 4--8) the data qubit is biased with $P(\mathrm{data}=1)\in[0.52,0.62]$. The CTC band exit (row 3 $\to$ row 4) is the dominant catcher-flagged sharp Hamming transition.}\label{tab:batch6}
\end{table}

\begin{figure}[h]
\centering
\includegraphics[width=0.95\textwidth]{figures/knopp_marrakesh_batch6.pdf}
\caption{Hardware-vs-simulator agreement on the Knopp Drive batch 6 sweep. Sim points (circles) and HW points (squares) overlay across the band exit. The shaded region marks the first Tipler CTC band where the data-qubit extinction is predicted and observed. The hardware catcher verdict (\texttt{novel\_structure}, 3 sharp features) matches the simulator verdict exactly, with the primary sharp at \texttt{r3 -> r4} (step$=12$).}\label{fig:batch6}
\end{figure}

The hardware catcher verdict --- \texttt{novel\_structure} with 3 sharp Hamming transitions, primary at \texttt{r3 -> r4} step$=12$ --- matches the simulator verdict exactly. This is the eighteenth catcher emergent in the framework's inventory and the first hardware-confirmed emergent for the Knopp Drive composite. Combined with the prior batch 5 result (Tipler-pair extinction zone), it establishes that two distinct Knopp-Drive mechanisms (band gating and pair extinction) are independently hardware-validated on a near-term quantum processor.

\subsection{Quantitative band-edge fit (Kingston batch 7, 16-point sweep)}\label{ssec:band_edge}

Batch 6 demonstrated the qualitative band-gating signature in 8 r-points. To pin down the band edges quantitatively, we re-submitted the same Knopp Drive 6-qubit circuit (1 data + 5 path qubits) on a denser 16-point r-grid to \texttt{ibm\_kingston}, the third Heron-r2 chip in the IBM Quantum allocation, as batch 7 (job \texttt{d81b6rvoha1c73bk5ee0}, 8192 shots/point, dynamical decoupling enabled).

Figure~\ref{fig:band_edge_kingston} shows the result. A smoothed-step band-gating model
\begin{equation}\label{eq:envelope}
P(\mathrm{data}=1;\, r) = P_{\mathrm{idle}} + (P_{\mathrm{active}} - P_{\mathrm{idle}}) \cdot \sigma\!\left(\tfrac{r - r_{\mathrm{in}}}{w_{\mathrm{in}}}\right) \cdot \sigma\!\left(\tfrac{r_{\mathrm{out}} - r}{w_{\mathrm{out}}}\right)
\end{equation}
fit to the 16 Kingston data points yields band edges $r_{\mathrm{in}} = 2.657 \pm 0.002$ and $r_{\mathrm{out}} = 5.471 \pm 0.010$ at $0.1\%$ precision, band width $\Delta r = 2.815 \pm 0.010$, and a contrast $P_{\mathrm{active}} - P_{\mathrm{idle}} = 0.5942 \pm 0.0032$ corresponding to a signal-to-noise ratio of $183.5\sigma$. The reduced chi-squared of the single-step envelope is $\chi^2/\mathrm{dof} = 16.2$, indicating real structure inside the band beyond Poisson shot noise.

Augmenting the envelope with two Lorentzian internal resonances drops the fit to $\chi^2/\mathrm{dof} = 1.09$ (perfect agreement with shot-noise budget) at the cost of six additional parameters, with peak centres $r_1 = 3.01 \pm 0.50$ (sharp, $w_1 = 0.09$) and $r_2 = 4.47 \pm 11.8$ (broad, $w_2 = 0.83$). The inner peak is well constrained; the outer peak's centre is unconstrained but its presence is required by the fit. The Knopp Drive active band therefore hosts \emph{at least} two internal resonances rather than a featureless plateau, consistent with the Krasnikov ring $+$ $Q$-cavity feedback composition producing coupled modes inside the gated region.

\begin{figure}[h]
\centering
\includegraphics[width=0.95\textwidth]{figures/knopp_band_edge_kingston.pdf}
\caption{Knopp Drive batch 7 on \texttt{ibm\_kingston} (third Heron-r2 chip), 16 r-points $\in [1.05, 6.00]$, 8192 shots/point. Black points: HW with $1\sigma$ shot-noise error bars. Blue dashed: best-fit smoothed-step envelope (Eq.~\ref{eq:envelope}), $\chi^2/\mathrm{dof}=16.2$. Red: envelope plus two Lorentzian internal resonances at $r_1 \approx 3.0$ and $r_2 \approx 4.5$, $\chi^2/\mathrm{dof}=1.09$ (perfect to shot-noise). Vertical green/orange: fitted band edges $r_{\mathrm{in}}=2.66$ and $r_{\mathrm{out}}=5.47$. The catcher verdict on this scan is \texttt{novel\_structure} with one sharp Hamming transition at $r=2.80 \to 2.95$, coinciding with the inner band edge.}\label{fig:band_edge_kingston}
\end{figure}

\subsection{Cross-chip reproducibility on Kingston + Marrakesh}\label{ssec:cross_chip}

The identical batch-7 circuit was also submitted to \texttt{ibm\_marrakesh} as job \texttt{d81bq77tjchs73bmm8sg} (8192 shots/point with the same dynamical-decoupling and twirling options as Kingston). Both chips returned essentially identical band-gating curves: the chip-to-chip RMS difference $\langle (P_K - P_M)^2 \rangle^{1/2} = 0.0118$ is $1.94\sigma$ of the pooled shot-noise budget. Figure~\ref{fig:cross_chip} overlays both chips with shot-noise error bars and reports a combined-chip fit at tighter precision:
\[
r_{\mathrm{in}}^{\mathrm{combined}} = 2.6539 \pm 0.0015, \qquad
r_{\mathrm{out}}^{\mathrm{combined}} = 5.4693 \pm 0.0067.
\]
Both band edges are now pinned at $\le 0.12\%$ relative precision across two independent Heron-r2 chips. Cross-chip residuals (lower panel of Fig.~\ref{fig:cross_chip}) are largest at the inner band edge (r $\approx 2.45$–$2.65$), where small calibration differences propagate amplified; the idle floor and active-band plateau agree within shot noise.

\begin{figure}[h]
\centering
\includegraphics[width=0.85\textwidth]{figures/knopp_cross_chip.pdf}
\caption{Cross-chip reproducibility: Kingston (blue circles) and Marrakesh (red squares) batch-7 data, identical circuit, both at 8192 shots/point. Black curve: combined-chip smoothed-step fit, $r_{\mathrm{in}} = 2.654$, $r_{\mathrm{out}} = 5.469$. Lower panel: cross-chip residuals normalised to pooled shot-noise $\sigma$. RMS $= 1.94\sigma$, consistent with a small chip-specific systematic on top of statistical shot noise. The Knopp Drive band-gating signature is reproduced on two independent Heron-r2 processors.}\label{fig:cross_chip}
\end{figure}

\section{Comparison with the canonical warp-drive families}\label{sec:comparison}

\begin{figure}[h]
\centering
\includegraphics[width=0.95\textwidth]{figures/knopp_warp_comparison.pdf}
\caption{Integrated exotic-matter requirement for an Earth--Mars equivalent journey ($L=0.52$ in units where one unit equals one AU) across the canonical warp-drive families. The Knopp Drive (blue) requires $|E_{\mathrm{neg}}| = 0$ exactly, whereas the Alcubierre, Krasnikov, and Bobrick--Martire ($m_{\mathrm{ADM}}=-1$) constructions each require a finite positive budget. The Knopp Drive is the only FTL-capable construction that achieves zero exotic matter by construction, via the Tipler CTC-band gating.}\label{fig:comparison}
\end{figure}

Figure~\ref{fig:comparison} compares the integrated exotic-matter requirement for an Earth--Mars equivalent journey ($L=0.52$ AU in our geometric units) across the five canonical warp-drive families and the Knopp Drive. Of the eight configurations shown, only three achieve $|E_{\mathrm{neg}}|=0$: the two Knopp Drive entries (by Tipler-band gating, FTL-capable) and the Lentz subluminal claim / Bobrick--Martire $m_{\mathrm{ADM}}=+1$ (subluminal, no FTL property). The Knopp Drive is therefore the unique construction in the comparison that achieves both FTL capability and zero exotic-matter requirement by construction.

\section{Catcher-validated emergent inventory}\label{sec:emergents}

The Systroph\=e framework has logged twenty-three Knopp-Drive-relevant address-space-catcher-verified emergents. We collect them in Table~\ref{tab:emergents}. Two additional emergents (3-SAT phase transition recovery and Goldbach 3-band comet) live in the companion Millennium-problem catcher paper \texttt{paper/millennium\_catcher.pdf}.
\begin{table}[h]
\centering
\small
\begin{tabular}{rlll}
\toprule
\# & Domain & Source & Catcher verdict / location \\
\midrule
1 & D-CTC & \texttt{phase\_a} & iter regime change at $\dim_{\mathrm{CR}}{\ge}6,\dim_{\mathrm{CTC}}{\ge}4$ \\
2 & D-CTC & \texttt{phase\_oqr} & Haar vs Clifford eigenvalue divergence \\
3 & D-CTC & \texttt{phase\_e} & purity-quantile $\sigma_{\min}$ threshold \\
4 & D-CTC & \texttt{phase\_f} & purity-quantile $\sigma_{\min}^{\mathrm{joint}}$ threshold \\
5 & D-CTC & \texttt{phase\_g} & purity-quantile best-overlap threshold \\
6 & D-CTC & \texttt{E4\_trichotomy} & basin boundaries (rows $1{\to}2$ and $8{\to}9$)\\
7 & HW & Marrakesh batch 5 & extended extinction zone $\delta\in[7\pi/8,3\pi/2]$\\
8 & Phase module & \texttt{cauchy\_stability} & sharp at $r=2.28$ ($27\times$ median)\\
9 & Phase module & \texttt{spinor\_monodromy} & sharp at $r=1.67$ ($14\times$)\\
10 & Phase module & \texttt{synchrotron\_analog} & sharps at $r=1.36, 3.52, 7.84$\\
11 & Phase module & \texttt{kg\_scattering} & sharp at $r=1.976$ ($16\times$)\\
12 & Phase module & \texttt{photon\_sphere} & sharp at $r=1.976$ ($32\times$); \textbf{coincides with \#11}\\
13 & Phase module & \texttt{berry\_phase\_lp} & sharp at $r=5.58$ ($13\times$)\\
14 & Cover/cascade & \texttt{stress\_zn\_closure} & transitions across $N\in\{2,3,5,6,7,11,17\}$\\
15 & Warp hybrid & \texttt{tipler\_krasnikov\_hybrid} & CTC-band on/off NEC gating\\
16 & Phase module & \texttt{optical\_fiber\_analog} & analog horizon onset at $v_{\mathrm{end}}{=}0.669$\\
17 & Feedback shell & \texttt{feedback\_amplified\_shell} & critical $Q\approx 7.86$ threshold\\
18 & Composite & \texttt{knopp\_drive} & sharp at $r=12$ (band re-entry)\\
19 & Warp ring & \texttt{krasnikov\_ring} & N=2 pair fragile to phase noise at $\sigma\approx 2.058$~rad\\
20 & HW & Marrakesh batch 6 & Knopp Drive band gating HW-confirmed on \texttt{ibm\_marrakesh}\\
21 & HW & Kingston batch 7 & Band edges $r_{\mathrm{in}}=2.657 \pm 0.002$, $r_{\mathrm{out}}=5.471 \pm 0.010$ at $\mathrm{SNR}=184\sigma$, 2 internal resonances ($\chi^2/\mathrm{dof}=1.09$)\\
22 & HW cross-validation & \texttt{knopp\_derivative\_catcher\_validation} & Derivative catcher independently identifies Kingston batch 7 substructure: sharps at $r\in\{3.10,3.90,4.60\}$\\
23 & HW cross-chip & \texttt{knopp\_cross\_chip} & Kingston + Marrakesh batch 7 agree at RMS$=1.94\sigma$ of pooled shot noise; combined-chip fit $r_{\mathrm{in}}=2.654\pm0.002$, $r_{\mathrm{out}}=5.469\pm0.007$\\
\bottomrule
\end{tabular}
\caption{The twenty-three Knopp-Drive-relevant catcher-verified emergents. Items 1--6 are from the Deutsch-CTC family. Items 7 and 20 are hardware-validated on the IBM Quantum \texttt{ibm\_marrakesh} 156-qubit Heron-r2 processor. Items 8--13 are radial sharps in the Systroph\=e LP supercritical exterior; the coincidence of \#11 and \#12 at $r=1.976$ is partially explained by their shared $F/L$ dependence (not truly independent diagnostics). Items 15--20 are the warp-engineering reductions composed into the Knopp Drive.}\label{tab:emergents}
\end{table}

The coincidence of \texttt{kg\_scattering} (\#11) and \texttt{photon\_sphere} (\#12) at the \emph{same} radius $r=1.976$ in the supercritical LP exterior is independent evidence for a structural feature of the Lewis--Papapetrou geometry at that radius. Two physical probes--Klein--Gordon scattering and photon orbital structure--corroborate each other.

\section{Steerability}\label{sec:steering}

The horn-toroidal twist of Section~\ref{ssec:horn} supplies a continuous steering vector
\begin{equation}
\bm{p}_{\mathrm{steer}}(\epsilon,\theta_{0}) = R^{2}\epsilon|m_{\mathrm{ADM}}|\bigl(\cos\theta_{0},\sin\theta_{0}\bigr) + O(\epsilon^{2}).
\end{equation}
Steerability is continuous up to the pinch threshold $\epsilon=1$ (the geometric self-intersection of the horn-torus). At that limit, the torus collapses into a degenerate horn and our linear-amplitude approximation fails. For $\epsilon\in[0,0.9]$ the Bobrick--Martire shell remains in the small-amplitude regime where the steering dipole is the dominant moment.

\section{Pfenning--Ford compatibility}

The Knopp Drive composite is Pfenning--Ford-compatible by construction: the $Q$-cavity feedback trades amplitude for duration along the bound $|E_{\mathrm{shell}}|\tau\ge 3/(32\pi^{2}\sigma^{2})$, the Tipler gate reduces \emph{both} amplitude and integrated duration in the same way (so the product is preserved), and the horn-twist contributes only a $O(\epsilon)$ angular modulation. We verify compatibility on every reported configuration; the \texttt{knopp\_drive} module returns the explicit boolean \texttt{pfenning\_ford\_compatible} on every budget call.

\section{Reversible-ratcheting directional bias}\label{sec:bias}

\subsection{The reciprocity obstruction}
The composite exotic-matter budget of Section~\ref{sec:composite} is \emph{independent} of the horn-twist axis $\theta_{0}$: only the steering vector $\bm{p}_{\mathrm{steer}}$ rotates with $\theta_{0}$, while $E_{\mathrm{neg}}^{\mathrm{comp}}$ does not. A pendulum that swings $\theta_{0}$ symmetrically therefore produces zero net impulse,
\begin{equation}
\bm{J}=\oint \bm{p}_{\mathrm{steer}}\,dt = R^{2}|m_{\mathrm{ADM}}|\,\epsilon\oint(\cos\theta_{0},\sin\theta_{0})\,d\theta_{0}=0,
\end{equation}
the warp-bubble analogue of Purcell's scallop theorem: a reciprocal deformation cycle yields no translation. Any directional bias must be \emph{manufactured} by breaking the reciprocity of the swing.

\subsection{The rising-floor ratchet}
We break reciprocity with a port of the Pareto ratchet proven in the TriCameral control stack. Each pendulum cycle is a \emph{power stroke} ($\epsilon=\epsilon_{\mathrm{hi}}$, worldline outside the band, exotic matter spent and steering thrust produced) followed by a \emph{recovery stroke} routed through the Tipler band, where the gate factor $\to0$ makes the return free. A two-metric ratchet on (displacement $d$, pinch-safety $s=1-\epsilon_{\mathrm{hi}}$) accepts a stroke only when the product $d\cdot\max(s,\delta)$ improves and, crucially, raises its acceptance floor monotonically,
\begin{equation}
\text{floor}\leftarrow\max\!\bigl(\text{floor},\ \text{best}\times f\bigr),\qquad f\in(0,1),
\end{equation}
so the configuration can never relax below $f$ of its best lean; a stroke regressing below the raised floor rolls back to the saved best state, giving closed-loop stability rather than open-loop runaway. The measured cost asymmetry between advancing and retreating one ratchet step, sourced entirely from \texttt{knopp\_budget}, rises with $f$ from $1.7\times$ (no pawl; the residual gate asymmetry alone) through $7.3\times$ ($f=0.85$) to $>\!100\times$ ($f=0.99$). The bias is reversible: re-anchoring with $\theta_{0}\!\to\!\theta_{0}+\pi$ locks the opposite direction.

\subsection{The shell as a surface capacitor}
The $Q$-cavity of Section~\ref{ssec:feedback} acts as an energy-accumulating surface capacitor. Under the parametric (dynamical-Casimir) pump it rings up as $E_{\mathrm{shell}}(t)=E_{\mathrm{shell}}(0)e^{gt}$ and at saturation holds only
\begin{equation}
E_{\mathrm{peak}}=|E_{\mathrm{neg}}^{\mathrm{bare}}|/Q
\end{equation}
sustained over the cavity lifetime $\tau=Q/f_{0}$. The instantaneous negative-energy charge present on the bubble is thus reduced by a factor $Q$ and the sustained pump power by $1/Q^{2}$: one never marshals the full requirement at once, but trickle-charges a small standing charge. The negative energy itself is the parametrically squeezed Casimir vacuum (the dynamical Casimir effect~\cite{Wilson2011}), \emph{not} a reservoir of exotic matter; static parallel-plate Casimir is quantitatively insufficient, and only the amplified route reaches the budget while respecting the quantum inequality.

\subsection{Ford--Roman quantum-interest ledger}
Negative energy borrowed for a hold time $\tau$ must be repaid by a larger positive pulse (the Ford--Roman quantum-interest conjecture~\cite{FordRoman1999}). Modelling the premium as $r=(\tau/\tau_{0})^{\alpha}-1=Q^{\alpha}-1$ with pulse width $\tau_{0}=1/f_{0}$, the per-stroke repayment at $\alpha=1$ is
\begin{equation}
E_{\mathrm{repay}}=\frac{|E_{\mathrm{neg}}^{\mathrm{bare}}|}{Q}\,(1+r)=|E_{\mathrm{neg}}^{\mathrm{bare}}|,
\end{equation}
\emph{independent of $Q$}: amplification removes the pump overhead $1/Q$ but never the quantum-interest \emph{principal}. The total per-stroke ledger $E=E_{\mathrm{pump}}+E_{\mathrm{repay}}=|E_{\mathrm{neg}}^{\mathrm{bare}}|(1+1/Q)$ therefore falls toward an irreducible floor as $Q\to\infty$ (within $+0.1\%$ at $Q=10^{3}$). For superlinear interest $\alpha>1$ the cost per unit displacement instead has an interior optimum at $Q^{*}=(\alpha-1)^{-1/\alpha}$.

\subsection{SI feasibility floor}
Converting the irreducible principal to SI via the textbook geometrised estimate $E_{\mathrm{geom}}\simeq\kappa(v_{s}/c)^{2}R^{2}/\sigma$ ($\kappa\approx1/12$) and $c^{4}/G=1.21\times10^{44}\,\mathrm{J/m}$, a one-metre luminal bubble ($\sigma=R$) carries a floor of $\approx6.5\times10^{44}\,\mathrm{J}\approx3.8$ Jupiter mass-energies, scaling as $R^{2}/\sigma$. This exceeds the negative-energy budget of a state-of-the-art squeezed-vacuum source by $\sim\!60$ orders of magnitude. The ratchet and capacitor make the \emph{peak charge} and \emph{pump power} arbitrarily small, but this \emph{total} is the same irreducible wall every warp metric encounters. The full controller is implemented in the \texttt{systrophe.knopp\_ratchet} module, with the address-space catcher returning \texttt{smooth} on the bias-energy curve (a continuously tunable control, not an emergent transition).

\section{The irreducible energy floor, the wormhole map, and the buildable residue}\label{sec:floor}

The bias controller (Section~\ref{sec:bias}) lowers the \emph{peak} negative-energy charge and pump power but not the time-integrated Ford--Roman principal. We close, from first principles, the question of whether \emph{any} route lowers that principal, and identify the one capability that is genuinely buildable.

\subsection{Geometry does not lower the principal}
The principal scales as $E\sim(c^{4}/G)\,(v_{s}/c)^{2}R^{2}/\sigma$, so the Van den Broeck~\cite{VanDenBroeck1999} trick---decoupling the energy-driving shell radius from the passenger pocket---appears to help. Calibrating the neck blow-up against the exact Einstein-tensor integral of the conformally-flat static pocket, $E_{\mathrm{pocket}}=\tfrac12\!\int(B'^{2}/B)r^{2}\,dr$, the energy scales as $B_{\max}$ (not $\ln^{2}B_{\max}$), so the blow-up dominates the shell and re-inflates the floor to $\sim(c^{4}/G)R_{\mathrm{pocket}}$. A calculus-of-variations argument (Euler--Lagrange minimiser $B=(1+c/r)^{2}$, isotropic Schwarzschild) shows the thickest wall is optimal yet still gives $E_{\min}\approx2(c^{4}/G)\rho_{\mathrm{use}}$; the direct Einstein-tensor integral of a \emph{regular} pocket gives an exotic (NEC-violating) energy $\approx5(c^{4}/G)\rho_{\mathrm{use}}$. The interior stays flat and habitable, but the floor is Jupiter-scale and irreducible across all wall shapes.

\subsection{The wormhole map: energy wall $=$ entanglement wall}
Mapping the traversable-wormhole routes, the classical Morris--Thorne exotic energy and the quantum ER$=$EPR~\cite{MaldacenaSusskind2013} requirement are dual: a throat of radius $r_{t}$ needs either $\sim(c^{4}/G)r_{t}$ of exotic energy \emph{or} $\sim(r_{t}/\ell_{P})^{2}$ ebits of pre-shared entanglement (Ryu--Takayanagi $S=A/4G_{N}$). The classical exotic energy equals exactly half the ER$=$EPR dual black-hole mass-energy at every scale---both equal assembling a black hole of radius $r_{t}$. For matter transport the wall is the same Bekenstein bound; only \emph{information} traverses cheaply.

\subsection{Hardware-validated ER$=$EPR channel}
The information channel is buildable. Under ER$=$EPR the shared entanglement is the bridge; sending a qubit through it (Gao--Jafferis--Wall~\cite{GaoJafferisWall2017} teleportation) is the operational traversal. We verify, from first principles, that the Maldacena--Qi coupling $V=\tfrac1n\sum_jZ_j^LZ_j^R$ is the operator-size operator on the EPR state, $V\,P_L|\mathrm{EPR}\rangle=(1-2\,\mathrm{size}(P)/n)\,P_L|\mathrm{EPR}\rangle$ (residual $10^{-17}$); a real SYK scrambler activates the size-winding channel where a Haar scrambler is inert, but the faithful dense-SYK wormhole transpiles to depth $11570$ ($3816$ two-qubit gates) and cannot run clean on current hardware. The deterministic coherent channel does: executed on IBM Heron-r2 hardware with a live-calibration best-qubit pipeline and error mitigation, the entanglement-teleportation Bell fidelity reaches $\mathbf{0.958}$ (\texttt{ibm\_kingston}, zero-noise extrapolated; $0.918$ on \texttt{ibm\_marrakesh}), well above the $0.5$ classical bound. This is entanglement-mediated quantum information transport---a real quantum-network primitive---\emph{not} faster-than-light and \emph{not} matter transport.

\section{Monetisation pathway}

The Knopp Drive composite provides four independently-quantified engineering bounds, each directly relevant to warp-drive physical realisation efforts:
\begin{enumerate}[leftmargin=*]
\item \textbf{Geometric exotic-matter zero}: a Krasnikov-tube craft routed through a Tipler CTC band requires \emph{zero} exotic matter for as long as the worldline lies inside the band. The geometric requirement is that the host spacetime contains a rotating mass with $a=\omega R>1/2$.
\item \textbf{Power-budget law}: feedback-amplified warp shells scale sustained drive power as $1/Q^{2}$ in the high-$Q$ limit, with a hard transition at $Q\approx 7.86$ (catcher emergent \#17) below which feedback is ineffective.
\item \textbf{Steering law}: continuous steering at $|\bm{p}|=R^{2}\epsilon|m_{\mathrm{ADM}}|$ up to $\epsilon\to 1$, with no threshold-locked discontinuity.
\item \textbf{Pfenning--Ford fence}: the composite saturates but never violates the quantum inequality.
\end{enumerate}

These four bounds are jointly novel and are reduced to an executable Python module (\texttt{systrophe.knopp\_drive}) with full numerical implementation, tests, and an example walkthrough.

\section{Toroidal Knopp Drive: finite-length realisation via a counter-rotating Kerr binary}\label{sec:toroidal}

The idealised Tipler source --- an infinite, rigid, axisymmetric dust column --- is the single largest obstruction to physical realisability of the Knopp Drive composite. Aguilera Katayama (April 2026, DOI~10.13140/RG.2.2.16820.82568) recently proposed a finite-length effective realisation in which the infinite cylinder is replaced by the inter-horizon toroidal domain of a gravitationally bound binary of two near-extremal Kerr black holes with maximally antiparallel (counter-rotating) spins. The composite Lense--Thirring frame-dragging in the equatorial toroidal domain is diffeomorphic --- in a coarse-grained effective sense --- to the Bonnor Case~III exterior's CTC bands. This section integrates that construction into the Knopp Drive stack and reports a self-consistent reduced engineering budget.

\subsection{Effective backend: \texttt{EffectiveToroidalKerrBinary}}

For two equal-mass Kerr holes with masses $M_{1}=M_{2}=M$, dimensionless spin $\chi\approx 1$, antiparallel along the binary axis, and coordinate separation $d$, the leading-order Lense--Thirring angular velocity in the midplane ($z=0$) at cylindrical radius $\rho$ from the axis is, by linear superposition,
\begin{equation}
\Omega_{\mathrm{eff}}(\rho) \;=\; \sum_{i=1,2}\frac{2\,a_{i}M_{i}}{r_{i}^{3}}
\;\approx\; \frac{4\chi M^{2}}{[(d/2)^{2}+\rho^{2}]^{3/2}}\qquad(G=c=1),
\end{equation}
with $r_{i}=\sqrt{(d/2)^{2}+\rho^{2}+z^{2}}$. The two contributions add constructively because the spins are antiparallel and the field is gravitomagnetic. The effective Tipler tilt --- the binary analogue of $T(r)=|K(r)|/|F(r)|$ in the Bonnor Case~III exterior --- is
\begin{equation}\label{eq:Teff}
T_{\mathrm{eff}}(\rho) \;\equiv\; \frac{|g_{t\phi}^{\mathrm{eff}}|}{|g_{tt}^{\mathrm{eff}}|}
\;\approx\;\frac{\Omega_{\mathrm{eff}}(\rho)\,\rho^{2}}{1-2\Phi_{\mathrm{eff}}(\rho)}
\;\xrightarrow[\text{outer region}]{}\;\frac{4\chi M^{2}\rho^{2}}{[(d/2)^{2}+\rho^{2}]^{3/2}}.
\end{equation}

The Tipler gate of \S\ref{ssec:tipler} carries over unchanged:
$g_{\mathrm{Tipler}}^{\mathrm{eff}}(\rho)=\max\bigl(1-c\,T_{\mathrm{eff}}(\rho),0\bigr)$.

\subsection{Toroidal CTC band: existence threshold}

Setting $T_{\mathrm{eff}}(\rho)=1$ and substituting $x=\rho/(d/2)$, $k=2M/d$ (we set $\chi=1$):
\begin{equation}
(2kx)^{2}\;=\;(1+x^{2})^{3/2}.
\end{equation}
The minimum of the right-hand side over the left-hand side, $\min_{x>0}\,(1+x^{2})^{3/2}/x^{2}$, occurs at $x^{2}=2$ and equals $3\sqrt{3}/2 \approx 2.598$. The band-existence condition is therefore
\begin{equation}\boxed{\;\;k \;=\; \frac{2M}{d} \;\geq\; k_{\rm crit} \;=\; \sqrt{\frac{3\sqrt{3}}{8}} \;\approx\; 0.806.\;\;}\end{equation}
A toroidal CTC band exists \emph{only} when $d\lesssim 2.48\,M$. The Aguilera Katayama paper's quoted reference configuration ($M=10^{6}M_{\odot}$, $d=10M$, $k=0.2$) lies \emph{below} this threshold and does \emph{not} admit a band in the leading-order LT approximation; the working configuration requires tight ($k\gtrsim 0.806$), maximally counter-rotating binaries.

\subsection{Composite budget in the toroidal regime}

Inside the toroidal CTC band ($T_{\mathrm{eff}}\geq 1$, $g_{\mathrm{Tipler}}^{\mathrm{eff}}=0$), the master composite formula of \S\ref{sec:composite} survives with $T(r)\to T_{\mathrm{eff}}(\rho)$:
\begin{equation}\label{eq:E_toroidal}
\bigl|E_{\mathrm{neg}}^{\mathrm{Knopp\text{-}Toroidal}}\bigr| \;=\;
\bigl|E_{\mathrm{Krasnikov}}(\alpha_{\mathrm{wall}},\sigma)\bigr|
\cdot g_{\mathrm{Tipler}}^{\mathrm{eff}}(\rho)
\cdot \frac{1}{Q^{2}}
\cdot (1+\epsilon).
\end{equation}
Classically inside the band, $|E_{\mathrm{neg}}^{\mathrm{Knopp\text{-}Toroidal}}|=0$. The chronology-protection counter-flux from semiclassical back-reaction is estimated as
\begin{equation}
E_{\rm BR}\;\sim\;\lambda \cdot \frac{1}{M^{4}}\cdot\frac{\rho^{2}d}{2880\pi^{2}},
\end{equation}
with $\lambda$ a small $\mathbb{Z}_{3}$-cover prefactor (default $10^{-4}$). The Newton--Kantorovich loop ($\S\ref{sec:bounds}$) is expected to converge to a self-consistent fixed point in which the back-reaction flux is absorbed by the $Q$-cavity once $Q\gtrsim Q_{\rm thr}=\sqrt{|E_{\mathrm{Krasnikov}}|/E_{\rm BR}}$. For the working configuration below this threshold is at the level of $10^{2}$--$10^{3}$.

\subsection{Working configuration}

A representative configuration where $g_{\mathrm{Tipler}}^{\mathrm{eff}}=0$ classically (tight, maximally counter-rotating binary in band):
\begin{center}
\begin{tabular}{lll}\toprule
parameter & value & meaning \\\midrule
$M$ & $1$ (geometric units) & individual BH mass \\
$d$ & $2M$ & axial separation ($k=1>k_{\rm crit}$) \\
$\chi$ & $1$ & near-extremal antiparallel spin \\
$\rho_{\rm orbit}$ & $1.5\,M$ & orbit radius inside band \\
$Q$ & $100$ & cavity quality factor \\
$\epsilon$ & $0.01$ & horn-twist amplitude \\\midrule
band edges & $[0.65,\,3.57]\,M$ & numerically solved \\
$T_{\rm eff}(1.5M)$ & $1.54$ & inside band \\
$g_{\rm Tipler}^{\rm eff}$ & $0$ & classical zero \\
composite $|E_{\rm neg}|$ & $0$ & classical \\
back-reaction $E_{\rm BR}$ & $1.6\times 10^{-8}$ & semiclassical residual \\\bottomrule
\end{tabular}
\end{center}

The full toroidal Knopp budget is implemented in \texttt{systrophe.knopp.knopp\_toroidal} (\texttt{EffectiveToroidalKerrBinary}, \texttt{knopp\_toroidal\_budget}). The IBM hardware-validation circuits of \S\ref{sec:hw} carry over unchanged: the Floquet eigenmode encoding maps to the toroidal-band frequencies via the diffeomorphism, and the band-edge extinction signature is the same up to a frequency rescaling.

\subsection{Honest caveats specific to the toroidal extension}

\begin{itemize}[leftmargin=*]
\item The Lense--Thirring superposition is linear; corrections from the near-horizon strong field can shift $T_{\mathrm{eff}}$ by $O(1)$ via the $(1-2\Phi_{\mathrm{eff}})$ denominator, and may eliminate the band entirely when included.
\item Stability of a near-extremal antiparallel-spin binary against orbital decay and merger is itself a speculative claim of the framework. The working configuration above is a classical kinematic statement, not a dynamical viability result.
\item The diffeomorphic isomorphism to Bonnor Case~III is an effective-correspondence claim, not a proven global isometry. The exact composite spacetime is not in closed form.
\end{itemize}

\subsection{Self-consistent semiclassical back-reaction via Newton--Kantorovich}\label{ssec:toroidal_nk}

The Aguilera Katayama framework leaves the back-reaction term as a dimensional ansatz
$E_{\rm BR}\!\sim\!\lambda\, M^{-4}\,\rho^{2}d/(2880\pi^{2})$ with a free prefactor $\lambda$.
We replace it here by a Newton--Kantorovich iteration of the band-edge equation under an actual Polyakov--Boulware vacuum source.

On the 2D radial slice $ds^{2}_{2\rm D}=-F(\rho)\,dt^{2}+d\rho^{2}$ with $F(\rho)=1-2\Phi_{\rm eff}(\rho)=1+4M/\sqrt{(d/2)^{2}+\rho^{2}}$, the Boulware vacuum stress
\begin{equation}
\langle T_{tt}\rangle_{B}(\rho)\;=\;-\frac{1}{24\pi}\!\left[F''(\rho)-\frac{F'(\rho)^{2}}{2F(\rho)}\right]
\end{equation}
is the exact Davies--Fulling--Unruh result in the conformally-flat 2D sector. We define the back-reacted Tipler tilt
\begin{equation}
T_{\rm eff}^{\rm BR}(\rho;\lambda)\;=\;T_{\rm eff}^{\rm classical}(\rho)\;-\;\lambda\,\langle T_{tt}\rangle_{B}(\rho)
\end{equation}
and solve the band-edge condition $T_{\rm eff}^{\rm BR}(\rho;\lambda)=1$ via \texttt{newton\_kantorovich\_1d} for each edge. Bisection over $\lambda$ identifies $\lambda_{\rm crit}$ -- the back-reaction strength above which the band collapses.

For the working configuration ($M\!=\!1$, $d\!=\!2M$, $k\!=\!1$):
\begin{itemize}[leftmargin=*]
\item Classical band: $\rho\in[0.6523,\,3.5719]\,M$.
\item $\langle T_{tt}\rangle_{B}$ is sign-changing: $+6.8\!\times\!10^{-3}$ at the inner edge, $-1.6\!\times\!10^{-3}$ at the outer.
\item Newton--Kantorovich on each edge converges in $\lesssim\!20$ iterations.
\item $\lambda_{\rm crit}\!\approx\!21.58$ -- the band closes for $\lambda\!\geq\!\lambda_{\rm crit}$.
\end{itemize}

The Q-cavity threshold follows from energy balance between the chronology-protection power flux and the cavity absorption rate:
\begin{equation}
\boxed{\;\;Q_{\rm thr}\;=\;\sqrt{\frac{|E_{\rm Krasnikov}|\,\omega_{0}\,A_{\rm band}}{|\langle T_{tt}\rangle_{B}|_{\rm inner}}}\;\;}
\end{equation}
For the working configuration with $|E_{\rm Krasnikov}|=\omega_{0}=1$, this evaluates to $Q_{\rm thr}\!\approx\!75.3$ -- within $13\%$ of the dimensional estimate ``$Q\!\gtrsim\!86$'' in the Aguilera Katayama framework, and now anchored in an explicit Polyakov vacuum stress rather than a free constant. The full implementation is \texttt{systrophe.knopp.knopp\_toroidal\_backreaction} (\texttt{backreaction\_diagnostic}, \texttt{critical\_lambda}, \texttt{q\_threshold\_from\_balance}).

\subsection{Dynamical stability and gravitational-wave signature}\label{ssec:toroidal_stability}

The framework's most speculative claim is that two near-extremal counter-rotating Kerr black holes hold together long enough to host the toroidal CTC band. We quantify this with the classical-GR (Peters 1964 quadrupole + leading-order Kidder 1995 spin--spin) result.

\paragraph{Spin--spin coupling at the working point.} For maximal antiparallel spins $\bm{S}_{1}=+M^{2}\hat{n}$, $\bm{S}_{2}=-M^{2}\hat{n}$ aligned with the binary axis,
\begin{equation}
U_{\rm SS}\;=\;\frac{1}{r^{3}}\!\left[3(\bm{S}_{1}\!\cdot\!\hat{n})(\bm{S}_{2}\!\cdot\!\hat{n})-\bm{S}_{1}\!\cdot\!\bm{S}_{2}\right]
\;=\;-\frac{2\chi^{2}M^{4}}{r^{3}}.
\end{equation}
The ratio to the Newtonian orbital energy $E_{\rm orb}=-M^{2}/(2d)$ is $|U_{\rm SS}/E_{\rm orb}|=4\chi^{2}M^{2}/d^{2}$. For the working configuration ($M\!=\!1$, $d\!=\!2M$, $\chi\!=\!1$) this is $\mathbf{1.00}$ -- the binary is fully non-perturbative for spin--spin.

\paragraph{Inspiral lifetime.} The Peters merger time for a circular equal-mass binary is $t_{\rm merge}=(5/256)\,d^{4}/M^{3}$ (geometric units). For the working configuration this gives $t_{\rm merge}\!=\!0.3125\,M$, while the orbital period is $T_{\rm orb}\!=\!2\pi/\Omega_{\rm orb}\!=\!2\pi\sqrt{2}\,M^{1/2}\!=\!8.886\,M$, so the inspiral lasts only
\begin{equation}\boxed{\;\;N_{\rm orbits}\;=\;t_{\rm merge}/T_{\rm orb}\;\approx\;0.012\;\;}\end{equation}
-- the binary merges in about $1\%$ of a single orbit. The toroidal CTC band, which requires the binary to persist, cannot persist either.

\paragraph{GW signature.} The dominant quadrupole emission peaks at $f_{\rm GW}=\Omega_{\rm orb}/\pi\approx 0.159/M$, which in SI units (with $M$ in solar masses) is $f_{\rm GW}\approx 3.23\!\times\!10^{4}/M_{\odot}\,$Hz. For an $M\!=\!10\,M_{\odot}$ stellar binary this lands in the LIGO band ($\sim\!3.2\,$kHz); for $M\!=\!10^{6}\,M_{\odot}$ in the LISA band ($\sim\!3.2\!\times\!10^{-2}\,$Hz). The merger time in seconds for $M\!=\!10\,M_{\odot}$ is $\sim\!7.7\,\mu$s.

\paragraph{Verdict.} The classical-GR analysis falsifies the working configuration of the Toroidal Knopp Drive on dynamical grounds. The two regimes available are:
\begin{itemize}[leftmargin=*]
\item $d\!\lesssim\!2.48\,M$ (band exists, $k\!\geq\!k_{\rm crit}$): binary survives $<\!1$ orbit. NOT VIABLE.
\item $d\!>\!2.48\,M$ (binary survives many orbits): NO toroidal CTC band. ALSO NOT VIABLE.
\end{itemize}
Any rescue of the toroidal Knopp Drive must invoke (a) non-gravitational binding (an unmodelled force or composite structure that holds the binary tight against quadrupole decay), or (b) a beyond-leading-order GR effect that strongly suppresses inspiral in the extremal counter-rotating regime, or (c) a different binary topology (e.g., a non-equal-mass or non-aligned configuration). The full classical analysis is implemented in \texttt{systrophe.knopp.knopp\_toroidal\_stability} (\texttt{stability\_report}, \texttt{corrected\_merger\_time}, \texttt{gw\_frequency\_in\_hz}, \texttt{detector\_classification}).

\subsection{Rescue paths explored}\label{ssec:toroidal_rescue}

The three rescue paths flagged in \S\ref{ssec:toroidal_stability} -- non-gravitational binding, beyond-leading-order GR, and alternative binary topologies -- are tested concretely in \texttt{systrophe.knopp.knopp\_toroidal\_pn\_rescue}, \texttt{knopp\_toroidal\_asymmetric}, and \texttt{knopp\_necklace}. The standing verdicts:

\paragraph{(a) PN rescue.} At $d=2M$ ($v^{2}=M/r=0.5$), the 1PN luminosity correction coefficient $-3.71-0.73\nu\approx-3.89$ at $\nu=1/4$ flips the sign of the PN factor $1+a_{1}v^{2}\approx-0.95$. The PN series has broken down in the strong-field regime; no reliable PN truncation can be quoted. At larger separations $d\!\gtrsim\!5M$ the PN series is well-behaved but no CTC band exists there (k < k_crit). \textbf{Rescue path \#2 is closed.}

\paragraph{(b) Asymmetric topology.} A scan of $\{q,\alpha,d\}$ at $q\in\{1,0.5,0.1,0.01\}$, $\alpha\in\{\pi,0.9\pi,0.7\pi,0.5\pi\}$, $d\in\{1.5,2,2.5,5,10\}\,M$ (80 configurations) finds zero parameter points where the toroidal CTC band exists \emph{and} the binary survives a single orbit. Unequal-mass binaries gain inspiral lifetime (chirp mass shrinks as $q^{3/5}$) but lose CTC band coverage (kappa-factor degradation under misalignment, plus the band-existence threshold moves). \textbf{Rescue path \#3 is closed.}

\paragraph{(c) Multi-binary necklace.} A ring of $N$ counter-rotating Kerr binaries at angular positions $\phi_{k}=2\pi k/N$ extends the band geometrically: the necklace closes into a continuous CTC band along the ring once $N \geq N_{\rm pack}$, where $N_{\rm pack}$ depends on $R_{\rm ring}/d$. For $R_{\rm ring}=5M$, $d=2M$: $N_{\rm pack}=4$. However, each individual binary inherits the same Peters merger problem ($n_{\rm orbits}\approx 0.025$ per binary). The necklace does \emph{not} fix the inspiral; only mutual angular-momentum exchange or ring-mode tidal coupling could, and neither is in the leading-order construction. \textbf{The necklace extends band coverage geometrically but inherits the falsification.}

\paragraph{Net.} None of the three rescue paths flagged in \S\ref{ssec:toroidal_stability} closes the inspiral-vs-band gap under the classical-GR + leading-order PN analysis. Any rescue must come from a yet-unmodelled effect: non-gravitational binding force, full numerical-relativity extremal-spin suppression, or a fundamentally different binary topology. Each is documented in the corresponding module's docstring but not derived here.

\subsection{Quantum-gravity probes of the toroidal band}\label{ssec:toroidal_quantum}

Two probes from the broader Systrophe stack become well-defined when the CTC band is the \emph{finite} toroidal region rather than the infinite Tipler cylinder (where both diverge):

\paragraph{LQG area discretization.} The band's inner and outer boundaries are 2-tori of proper area $A=2\pi\rho L_{z}\sqrt{1-2\Phi_{\mathrm{eff}}(\rho)}$. The LQG quantization $A_{j}=8\pi\gamma\ell_{P}^{2}\sqrt{j(j+1)}$ (Immirzi $\gamma\approx 0.2375$) gives a half-integer spin $j$ for each boundary, with a discretization residual that scales as $A_{\mathrm{classical}}^{-1/2}$. For the working configuration ($M=1$, $d=2M$, $\rho\in[0.65,3.57]M$), $j_{\mathrm{inner}}\!\approx\!2.5$, $j_{\mathrm{outer}}\!\approx\!10.5$, with residuals of $3.3\%$ and $1.4\%$ respectively. The band width in Planck units is $\Delta\rho\approx 2.9\,\ell_{P}$ -- well above unity, so the band is resolvable by the spinfoam; the band would be \emph{protected by gravitational discreteness alone} if its width fell below the spin-network coarse-grain scale.

\paragraph{Holographic complexity (CV \& CA).} The toroidal volume $V_{\mathrm{band}}\!=\!\int_{\rho_{\mathrm{in}}}^{\rho_{\mathrm{out}}}\!2\pi\rho\sqrt{1-2\Phi}\,d\rho\cdot L_{z}$ gives a Complexity = Volume proxy
\begin{equation}
\mathcal{C}_{V}\;=\;\frac{V_{\mathrm{band}}}{G_{N}\,\ell_{\rm AdS}},\qquad \ell_{\rm AdS}\!\equiv\!d.
\end{equation}
A Complexity = Action proxy uses the gravitomagnetic-energy density:
\begin{equation}
\mathcal{C}_{A}\;\sim\;\frac{1}{\pi}\int_{V_{\mathrm{band}}}\Omega_{\mathrm{eff}}^{2}\,dV.
\end{equation}
The Lloyd bound caps the complexity-growth rate at $d\mathcal{C}/dt \le 2E/(\pi\hbar)$ with $E=2M$. For the working configuration this gives $d\mathcal{C}/dt_{\max}\approx 1.27$ and a CV-traversal time $\tau_{V}=\mathcal{C}_{V}/\dot{\mathcal{C}}_{\max}\approx 49\,M$. Both proxies vanish as $d/M\to 1/k_{\rm crit}$, where the band closes.

The full quantum-probe module is \texttt{systrophe.knopp.knopp\_toroidal\_quantum}, with combined diagnostic \texttt{toroidal\_quantum\_diagnostics(binary)}.

\section{Limitations and falsifiers}\label{sec:limits}

\paragraph{Linearised composition.} The four mechanisms are composed in the linearised (single-Krasnikov-tube perturbation of LP exterior) regime. Cross-products of perturbations are formally $O(G^{2})$ and not modelled.

\paragraph{Idealised Tipler source.} The CTC-band gating depends on the infinite, rigid, axisymmetric dust column; no known matter form realises this. Finite-length corrections are explored in \S\ref{sec:toroidal} via the counter-rotating Kerr binary toroidal realisation, which is buildable in principle but inherits its own speculative stability claims.

\paragraph{Quantum-inequality saturation.} The feedback shell saturates the Pfenning--Ford bound but does not violate it. Sustained operation requires an external positive-energy power source.

\paragraph{Horn pinch.} The horn-twist parameter $\epsilon$ must satisfy $\epsilon<1$ to avoid horn-torus self-intersection. The linear $\bm{p}\sim\epsilon$ scaling is reliable on $[0,0.9]$.

\paragraph{Irreducible energy floor.} The reversible-ratcheting bias controller (Section~\ref{sec:bias}) lowers the \emph{peak} negative-energy charge and the pump power without bound as $Q\to\infty$, but the Ford--Roman quantum-interest principal is invariant and sits at Jupiter-mass scale ($\sim10^{44}$--$10^{45}$ J for a metre-scale bubble), $\sim60$ orders of magnitude beyond laboratory squeezed-vacuum sources. The controller is an engineering convenience over an irreducible energy wall, not a route around it.

\paragraph{Chronology protection.} The Knopp Drive is pre-quantum: the chronology-protection conjecture~\cite{Hawking1992} is not engaged. The Deutsch-CTC analysis~\cite{Deutsch1991} in the Systroph\=e DCTC trilogy provides one route to a quantum-consistent CTC theory.

\paragraph{Numerical validation only.} All four mechanisms are validated by Python simulation and the address-space novelty catcher; the composite has not been demonstrated on quantum hardware. Marrakesh batch 5 verified the bubble-extinction property at the wall level on IBM \texttt{ibm\_marrakesh} (5/5 circuits, TV $<$ 0.05); the Knopp composite has not yet been mapped to a hardware experiment.

\section{Conclusion}

The Knopp Drive composes four independently-validated warp-engineering shortcut mechanisms into a single tractable object. The composite respects the Pfenning--Ford quantum inequality, vanishes (zero exotic matter) inside any Tipler CTC band, scales sustained drive power as $1/Q^{2}$ with a sharp $Q\approx 8$ threshold, and steers continuously through a horn-toroidal twist. A reversible-ratcheting pendulum bias controller (Section~\ref{sec:bias}) converts the direction-blind steering dipole into stable directed transport and books the journey against the Ford--Roman quantum-interest bound, which fixes an irreducible Jupiter-mass energy floor that amplification reduces in \emph{peak} and \emph{power} but cannot remove in \emph{total}. We further establish, from first principles (Section~\ref{sec:floor}), that \emph{no} route lowers that principal: Van den Broeck geometry re-inflates under exact Einstein-tensor calibration, the thick-wall variational optimum still costs $\sim(c^{4}/G)\rho_{\mathrm{use}}$, and the wormhole energy wall is dual to a Bekenstein entanglement wall. The honest residue is not matter transport but an \emph{information} channel: the ER$=$EPR teleportation primitive, hardware-validated on IBM Heron-r2 at $0.958$ Bell fidelity---a genuine quantum-network result, not a propulsion device. The full implementation lives in the \texttt{systrophe.knopp\_drive}, \texttt{knopp\_ratchet}, \texttt{wormhole\_map}, \texttt{size\_winding}, and \texttt{erepr\_channel} modules of the open-source \emph{Systroph\=e} package~\cite{SystropheRepo}.

\section*{Acknowledgements}

This work is part of the larger Systroph\=e framework. Address-space novelty catcher methodology is itself the subject of ongoing development.

\begin{thebibliography}{99}
\bibitem{Alcubierre1994}
M.~Alcubierre, \emph{The warp drive: hyper-fast travel within general relativity}, Class. Quantum Grav. \textbf{11} (1994) L73.
\bibitem{PfenningFord1997}
M.~J.~Pfenning, L.~H.~Ford, \emph{The unphysical nature of warp drive}, Class. Quantum Grav. \textbf{14} (1997) 1743.
\bibitem{MaldacenaSusskind2013}
J.~Maldacena, L.~Susskind, \emph{Cool horizons for entangled black holes (ER=EPR)}, Fortsch. Phys. \textbf{61} (2013) 781.
\bibitem{GaoJafferisWall2017}
P.~Gao, D.~L.~Jafferis, A.~C.~Wall, \emph{Traversable wormholes via a double trace deformation}, JHEP \textbf{12} (2017) 151.
\bibitem{Krasnikov1995}
S.~V.~Krasnikov, \emph{Hyperfast travel in general relativity}, Phys. Rev. D \textbf{57} (1998) 4760.
\bibitem{EverettRoman1997}
A.~E.~Everett, T.~A.~Roman, \emph{A superluminal subway: the Krasnikov tube}, Phys. Rev. D \textbf{56} (1997) 2100.
\bibitem{VanDenBroeck1999}
C.~Van Den Broeck, \emph{A `warp drive' with more reasonable total energy requirements}, Class. Quantum Grav. \textbf{16} (1999) 3973.
\bibitem{Lentz2021}
E.~W.~Lentz, \emph{Breaking the warp barrier: hyper-fast solitons in Einstein--Maxwell-plasma theory}, Class. Quantum Grav. \textbf{38} (2021) 075015.
\bibitem{BobrickMartire2021}
A.~Bobrick, G.~Martire, \emph{Introducing physical warp drives}, Class. Quantum Grav. \textbf{38} (2021) 105009.
\bibitem{Tipler1974}
F.~J.~Tipler, \emph{Rotating cylinders and the possibility of global causality violation}, Phys. Rev. D \textbf{9} (1974) 2203.
\bibitem{Hawking1992}
S.~W.~Hawking, \emph{The chronology protection conjecture}, Phys. Rev. D \textbf{46} (1992) 603.
\bibitem{Deutsch1991}
D.~Deutsch, \emph{Quantum mechanics near closed timelike lines}, Phys. Rev. D \textbf{44} (1991) 3197.
\bibitem{SystropheRepo}
C.~Knopp, \emph{Systroph\=e: a co-rotating Tipler-cylinder pair as a tunable time-travel harness}, \url{https://github.com/Zynerji/systrophe} (2026).
\bibitem{FordRoman1999}
L.~H.~Ford, T.~A.~Roman, \emph{The quantum interest conjecture}, Phys. Rev. D \textbf{60} (1999) 104018.
\bibitem{Wilson2011}
C.~M.~Wilson, G.~Johansson, A.~Pourkabirian, M.~Simoen, J.~R.~Johansson, T.~Duty, F.~Nori, P.~Delsing, \emph{Observation of the dynamical Casimir effect in a superconducting circuit}, Nature \textbf{479} (2011) 376.
\end{thebibliography}

\end{document}
